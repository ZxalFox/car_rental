{
  "name": "car-rental-gatsby",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "develop": "gatsby develop -H 0.0.0.0 -p ${GATSBY_PORT:-8001}",
    "build": "gatsby build",
    "serve": "gatsby serve -H 0.0.0.0 -p ${GATSBY_PORT:-8001}",
    "clean": "gatsby clean"
  },
  "dependencies": {
    "gatsby": "^5.13.3",
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  }
}
